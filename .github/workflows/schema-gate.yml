name: Schema Gate

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  frontmatter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run frontmatter gate
        run: python 00_SYSTEM/tools/check_frontmatter.py
